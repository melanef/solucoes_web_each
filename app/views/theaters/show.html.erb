<h1>Exibindo Cinema: <%= @theater.name %></h1>

<p>
    <strong>Endereço:</strong>
    <%= @theater.address %>, <%= @theater.address_number %>
</p>

<p>
    <strong>Cidade:</strong>
    <%= @theater.city %>
</p>

<p>
    <strong>Estado:</strong>
    <%= @theater.state %>
</p>

<h2>Salas</h2>
<% @theater.rooms.each do |room| %>
    <p>
        <strong>Nome:</strong> <%= room.name %>
    </p>
<% end %>

<%= link_to 'Voltar', theaters_path %> | <%= link_to 'Editar', edit_theater_path(@theater) %> | <%= link_to 'Editar Salas', theater_rooms_path(@theater) %>